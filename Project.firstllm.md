Legacy note

This repository is now a single Bun service that calls ElevenLabs Speech-to-Text (Scribe v2).

If you are looking for the older Whisper + Python sidecar design, keep it in a separate legacy branch.
